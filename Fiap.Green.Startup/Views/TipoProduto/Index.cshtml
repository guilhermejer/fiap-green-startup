@model IEnumerable<Fiap.Green.Startup.Models.TipoProduto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeTipoProduto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeTipoProduto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoProduto">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoProduto">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoProduto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
