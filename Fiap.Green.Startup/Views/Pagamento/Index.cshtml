@model IEnumerable<Fiap.Green.Startup.Models.Pagamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoPagamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DadosPagamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoPagamento.IdTipoPagamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DadosPagamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPagamento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPagamento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPagamento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
